FROM python:3.12-slim

WORKDIR /app

COPY src /app/src

ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app/src
ENV SYNCROOM_PORT=24873

EXPOSE 24873/tcp

CMD ["sh", "-c", "python -m syncroom.server --host 0.0.0.0 --port ${SYNCROOM_PORT}"]
